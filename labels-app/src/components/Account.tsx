import React, { FC } from 'react';
import { withRouter } from 'react-router';

const Account: FC = () => {

    return (
        <div>
            <p>Accountコンポーネントです</p>
        </div>
    )
};

export default withRouter(Account);